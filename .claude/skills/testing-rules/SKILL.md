---
name: testing-rules
description: Правила тестирования zerovibe-приложений — что и как покрывать (unit usecase + e2e транспорта). Используй при написании тестов, при добавлении фичи, или когда пользователь просит проверить покрытие.
---

# Правила тестов Zerovibe

Минимально достаточное покрытие, обязательное для каждой фичи. Два уровня:

## 1. Unit usecase (на фейк-репозитории)

На каждый сервис в `usecase` — тест с in-memory реализацией порта (фейк), без БД
и сети. Проверяй:
- успешный путь (создание/чтение возвращает ожидаемое);
- валидацию (пустые/слишком длинные поля → `ErrValidation`, проверка `errors.As`);
- бизнес-правила (порядок, лимиты, "не найдено" → `ErrNotFound`).

Фейк-репозиторий — простая структура со слайсом и реализацией методов порта.

## 2. E2E транспорта (httptest на локальном PostgreSQL)

На каждый раздел — тест полного стека (`pg → repo → usecase → web`). БД —
локальный PostgreSQL, каждому тесту своя схема: используй хелпер
`openTestPG(t)` из `transport/web/web_test.go` (он сам применяет миграции,
чистится через `t.Cleanup`, а без поднятого Postgres — пропускает тест). Проверяй:
- мутация возвращает HTML-ФРАГМЕНТ (без `<html>`, с нужным классом/содержимым);
- `GET` страницы возвращает полную страницу с данными;
- удаление убирает сущность;
- негатив: несуществующий id → 404.

Если раздел защищён входом — в тесте сначала засей и залогинь пользователя
(см. хелперы в `transport/web/web_test.go`), и шли запросы с его cookie. Для личных
данных добавь тест изоляции: пользователь B не видит и не может удалить данные A.

## Что НЕ тестировать

- Геттеры/тривиальные мапперы без логики.
- Сам `platform/pg` (подключение/мигратор) — он покрыт эталоном, не дублируй.

## Запуск

`go test ./...` + `go vet ./...` (или `make check`). Unit-тесты работают без
всего; e2e-тестам нужен ЛОКАЛЬНЫЙ PostgreSQL (`make pg-up` на машине
разработчика). **В твоей песочнице локального Postgres нет — e2e-тесты там
пропускаются сами** (общая облачная база не место для тестового мусора, так
задумано — не пытайся их «включить»). Все НЕпропущенные тесты должны проходить
ДО того, как скажешь «готово» — **красные тесты = не готово**. ВАЖНО: ошибки
SQL-запросов не ловятся ни компиляцией, ни пропущенными e2e — в песочнице их
видно ТОЛЬКО при живом запуске, поэтому после правок запросов ОБЯЗАТЕЛЬНО
прогони сценарий вживую (skill `run`).

## Принцип

Покрытие — не ради процентов, а ради уверенности: бизнес-логика (usecase) и
контракт HTTP+HTML (transport) проверены. Этого достаточно для приложения такого
класса; не раздувай тесты на инфраструктуру.
