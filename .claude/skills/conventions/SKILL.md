---
name: conventions
description: Конвенции проекта zerovibe (Go + HTMX + PostgreSQL, чистая архитектура) И правила общения с пользователем. Используй при ЛЮБОЙ работе над zerovibe-приложением — обсуждении идеи, написании или изменении кода (хендлеров, usecase, репозиториев, доменных сущностей, шаблонов), ответах пользователю.
---

# Конвенции Zerovibe

Приложение на стэке **Go + HTMX + PostgreSQL** с чистой архитектурой. Соблюдай эти
правила при генерации и изменении кода — они отражают эталонный скелет.

## Общение с пользователем (ВАЖНО — соблюдай всегда)

Пользователь Zerovibe — **создатель продукта, НЕ программист**. Он мыслит
продуктом и пользователями, а не кодом. Поэтому:

- **Технические решения принимай сам**, опираясь на эти конвенции и эталонный
  скелет. НЕ спрашивай пользователя про код, архитектуру, библиотеки, типы данных,
  схему БД, API, фреймворки, способ вёрстки и т.п. — это твоя зона ответственности.
- **Спрашивай только продуктовое** — и только когда это правда меняет результат
  для пользователя: что должно быть в приложении, как себя ведёт, что видит
  конечный пользователь, какие поля важны по смыслу. Если продуктовый выбор
  очевиден или некритичен — прими разумное умолчание и двигайся дальше, кратко
  упомянув что выбрал.
- **Говори на языке продукта, без жаргона.** Не «добавил usecase и репозиторий
  для сущности Expense с миграцией» → а «сделал, теперь можно добавлять и удалять
  траты, внизу появилась таблица и общая сумма».
- **НИКОГДА не называй технологии и внутреннюю кухню в разговоре. Это железное
  правило, не пожелание.**

  **Почему это критично (пойми механизм, а не просто запомни):** пользователь НЕ
  знает, что такое «база», «кластер», «кубер», «деплой», «контейнер». Для него это
  не «сложные слова» — это **полностью неизвестные слова**, ровно как если бы ты
  написал ему фразу на китайском или латыни. Он не понимает их не на 10%, а на 100%.
  Каждое такое слово для него = шум + сигнал «здесь что-то сложное, я не справлюсь».
  Это прямо разрушает саму идею платформы (приложение может сделать кто угодно).
  Технический термин в ответе пользователю — это всегда твоя ошибка, а не его пробел.

  **Под полным запретом** (примеры, не исчерпывающий список): название/тип БД
  (PostgreSQL, «база данных», «таблица», «миграция»), хранилище (volume, диск, PVC),
  язык/фреймворки (Go, HTMX, CSS), слои/архитектура (usecase, repository, domain,
  port, handler), инфраструктура (**Kubernetes, кубер, кластер, под, нода, ingress,
  Docker, контейнер, образ, registry, реестр, сборка образа**), деплой-внутрянка,
  любые англицизмы-термины и аббревиатуры (API, TLS, env, CI и т.п.).

  **Простой тест перед каждой фразой пользователю:** «поняла бы это моя бабушка,
  которая никогда не программировала?» Если в предложении есть слово, которое
  бабушка не использует в обычной жизни — убери его и скажи через то, что человек
  ВИДИТ и ДЕЛАЕТ: «данные сохраняются», «добавилась страница», «приложение
  опубликовано и доступно по ссылке», «всё работает». Если очень нужно про хранение
  — «данные приложения», без слова про БД.
- **Никогда не проси пользователя что-то сделать в коде/терминале руками.**
  Запуск, сборку, тесты, исправления делай сам. Публикацию приложения пользователь
  делает сам кнопкой «Опубликовать» на платформе — ты код НЕ деплоишь.
- **Плохие новости — на языке последствий, не причин.** Не «SQLITE_BUSY из-за
  гонки записи» → а «при одновременном сохранении могла теряться запись, починил».

Короткое правило: *продуктовые вопросы — пользователю; технические — себе.*

- **Проверь результат вживую сам ПЕРЕД тем, как сказать «готово».** После любой
  заметной работы (фича, экран) подними приложение и прогони сценарий сам —
  убедись, что реально работает, а не только собирается (skill `run`).
- **Покажи результат вайбкодеру ЖИВЫМ превью** (когда сделал что-то визуальное или
  человек просит «покажи») — панель с приложением рядом с чатом, обновляется на
  лету. Это НЕ публикация, а просмотр (skill `preview`). В облаке предпочитай его
  «расскажу словами»: человек видит приложение сам.
- **Публикацию (деплой) и первого админа делаешь ТЫ, не пользователь.** Кнопка
  «Опубликовать» просто присылает тебе сообщение «опубликуй» — дальше публикуешь
  ты через skill `deploy` (проверить → запросить публикацию → завести первого
  админа через /setup). Пользователь только называет желаемый поддомен и
  email/пароль владельца; никаких ссылок и кодов ему не показывай.

## Слои и зависимости (строго внутрь)

```
internal/
  domain/              сущности + инварианты + ошибки. ТОЛЬКО stdlib. Без БД, без HTTP, без тегов.
  usecase/             бизнес-логика + порты (интерфейсы репозиториев). Зависит только от domain.
  repository/pgrepo/   реализация портов поверх PostgreSQL (database/sql).
  platform/pg/         подключение к PostgreSQL + мигратор. Не трогать без необходимости.
  transport/web/       HTTP/HTML на HTMX. Зависит от usecase.
  admin/               встроенная админка: generic-CRUD по реестру дескрипторов (НЕ трогать ядро).
  adminres/            регистрация сущностей в админке (дескриптор на сущность; эталон note.go/user.go).
  adapter/             адаптеры портов: platformmail (почта), platformfiles (файлы на S3 через платформу).
cmd/server/            composition root: склейка слоёв.
```

- `domain` не импортирует ничего из проекта и никаких json/db-тегов не несёт.
- `usecase` объявляет порты (интерфейсы), реализации внедряются снаружи.
- Конвертация между представлениями — на стыках слоёв, не протекает вглубь.

## База данных: PostgreSQL через database/sql

- Репозитории работают со стандартным `*sql.DB`: `QueryRowContext`/`ExecContext`/
  `QueryContext`, плейсхолдеры — `$1, $2, …` по порядку аргументов.
- Новая строка: `INSERT ... RETURNING id` (id выдаёт база — колонка
  `bigint GENERATED ALWAYS AS IDENTITY`).
- Уникальность и связи — ограничениями в схеме (UNIQUE, REFERENCES);
  нарушение UNIQUE лови `isUniqueViolation(err)` → доменная ошибка.
- Время — `time.Time` напрямую (колонки timestamptz), NULL = «не было»
  (сканируй в `sql.NullTime`, хелперы from/toNullTime уже есть).
- «Была ли строка» после UPDATE/DELETE — `RowsAffected`; связанные шаги
  (проверка + запись) — в транзакции `BeginTx`.
- Полный свод с работающим кодом — package doc `internal/repository/pgrepo` и
  эталон Note (skill `new-feature`).

## Миграции

- Схема БД живёт ТОЛЬКО в файлах `internal/platform/pg/migrations/NNNNN_*.sql`
  (встроены через embed, применяются при старте, учёт — в `schema_migrations`).
- Новая таблица/колонка = НОВЫЙ файл миграции (следующий номер), а НЕ правка
  существующего и НЕ inline `CREATE TABLE` в коде репозитория. Так у уже задеплоенных
  приложений база корректно доедет до новой версии.
- В файле — обычный PostgreSQL DDL; операторы разделяются `;`; миграция
  применяется одной транзакцией; откатов нет, только вперёд.

## Готовое в шаблоне (не изобретать заново)

В эталоне УЖЕ есть, бери как данность:
- **Аутентификация**: пользователи, роли (гость→user→admin), вход/выход, сессии
  (cookie), восстановление пароля письмом, подтверждение почты, рейт-лимиты.
  Регистрация по умолчанию закрыта (`allow_signup`), подтверждение почты по умолчанию
  выключено (`require_email_verification`) — обе управляются настройками.
- **Настройки приложения**: типизированный реестр (config + secret) в
  `internal/domain/setting.go`, CRUD под admin (`/admin/settings`). Новая настройка =
  запись в реестре.
- **Админка из коробки** (`internal/admin` + `internal/adminres`): generic «бэкофис» —
  список/форма/CRUD/поиск/фильтры/сортировка/экспорт CSV для любой сущности по реестру
  дескрипторов. Доступна по `/admin` под ролью администратора. Стиль — собственный
  универсальный бэкофис (shadcn-на-CSS, `internal/admin/templates`), общий на все
  сущности; не переопределять per-сущность. Добавить сущность в админку = дескриптор
  в `adminres` + одна строка регистрации в `main.go` (см. skill `new-feature`, шаг 5.5).
- **Загрузка файлов** (`adapter/platformfiles`, порт `usecase.FileStore`): файлы
  end-users хранятся на S3 через платформу (presigned-URL, как почта — S3-креды в
  приложение не попадают). Локально — каталог `uploads/`. В админке тип поля
  `FieldFile` подключает загрузку автоматически. Сами байты в базе не хранить —
  только ключ файла.
- **Платформенные переменные**: при деплое платформа сама прокидывает в приложение
  `PLATFORM_API_URL`/`PLATFORM_API_KEY` (для отправки писем), `APP_BASE_URL`,
  `SECURE_COOKIE`. Креатор о них не думает — НЕ проси задавать их руками.

## HTMX (ключевой паттерн рендеринга)

### 🚫 ЖЕЛЕЗНОЕ ПРАВИЛО: интерфейс работает БЕЗ перезагрузки страницы — как SPA

ВСЕ действия пользователя на фронтенде (создать/изменить/удалить запись, поиск,
фильтр, сортировка, смена страницы, переходы между разделами, открытие форм/модалок)
происходят БЕЗ полной перезагрузки страницы. Никаких «классических» `<form method=post>`
с редиректом и перерисовкой всего экрана. Ощущение — как у одностраничного приложения.
Это нерушимое требование продукта, проверяй его на КАЖДОМ экране, который делаешь или
правишь. Достигается через HTMX (стэк уже подключён, ничего ставить не надо):

- интерактивные элементы несут `hx-get`/`hx-post`/`hx-put`/`hx-delete` + `hx-target` +
  `hx-swap`; браузер НЕ навигирует, htmx подменяет нужный кусок DOM ответом сервера;
- формы отправляются через `hx-post`/`hx-put` и возвращают фрагмент (обновлённую
  строку/таблицу/закрытую модалку), а НЕ редирект (303/Location);
- поиск/фильтр — `hx-get` с `hx-trigger` (напр. `keyup changed delay:300ms`), подменяет
  тело таблицы; пагинация/сортировка — тоже `hx-get` с подменой таблицы;
- удаление — `hx-delete`/`hx-post`, убирает строку из DOM (`hx-swap:outerHTML`/`delete`);
- адресная строка при необходимости синхронизируется через `hx-push-url`, чтобы работали
  «назад»/обновление, но сам переход — без полной перезагрузки.
- **Навигация между страницами (клики по ссылкам `<a href>`) — тоже без перезагрузки.**
  На `<body>` стоит `hx-boost="true"` (см. `layout.html`): htmx перехватывает клики по
  обычным ссылкам и грузит страницу в фоне, подменяя `<body>` — контент НЕ пропадает на
  миллисекунду, нет белой вспышки. НЕ снимай `hx-boost`. Обычные `<a href="…">` для
  внутренних переходов — это правильно (boost сделает их плавными), НЕ нужен `target=_blank`
  или `hx-get` на каждую ссылку. CSS-затухание `.htmx-swapping/.htmx-settling` в `app.css`
  усиливает плавность — не удаляй эти правила.

ПРОВЕРКА ГОТОВОГО ЭКРАНА (обязательно, skill `run`): открой в браузере и покликай —
переходы по ссылкам И действия (создать/удалить/фильтр) НЕ должны вызывать белого
моргания, пропадания контента на миг или полной перерисовки. Мигает → экран НЕ готов,
чини (чаще всего забыл, что boost работает только с обычными `<a>`/формами, а не с
`onclick`-редиректом через JS, или вернул полный layout вместо фрагмента на HX-Request).

### Как это устроено

- `GET` страницы — отдаёт ПОЛНУЮ страницу (layout + content) ТОЛЬКО при прямом заходе
  по URL (или `HX-Request` отсутствует). При `HX-Request` тот же роут может вернуть
  лишь фрагмент-контент для подмены без перерисовки каркаса.
- Мутации (`POST`/`PUT`/`DELETE`) возвращают РОВНО ТОТ HTML-фрагмент, который
  меняется (например, один элемент списка), а не всю страницу. htmx подменяет его
  на клиенте через `hx-target` / `hx-swap`.
- Шаблоны — `html/template` (stdlib), встроены через `embed`. Фрагмент = отдельный
  `{{define "..."}}`.
- **Оформление — обычный CSS** (`static/app.css`, вшивается в бинарь). Разметка
  семантическая, интерактив — HTMX (см. выше). Встроенная админка (`internal/admin`) —
  исключение: у неё собственные стили, её не трогаем.

## Ошибки

- Доменные ошибки — типы в `domain` (`ErrValidation`, `ErrNotFound`, ...).
- Транспорт мапит их в HTTP-коды в ОДНОЙ функции (`fail` в `transport/web`),
  через `errors.As` / `errors.Is`. Новую доменную ошибку добавляй туда же.

## Стиль Go

- Конструкторы-валидаторы в domain (`NewX`) — единственное место инвариантов сущности.
- `context.Context` первым аргументом во все методы, ходящие в БД/сеть.
- Без лишних зависимостей: предпочитай stdlib. БД — драйвер `pgx/v5` через database/sql.
- Каждый публичный тип/функция — с кратким комментарием на русском.

## Сохранение кода (git)

Код проекта версионируется через git (если в /workspace есть каталог `.git`).
**Сохранять код — твоя забота, никто не делает это за тебя.**

- **Закончил осмысленную правку → сохрани:** `git add -A && git commit -m "<коротко что сделал>" && git push`.
  Коммить осмысленными кусками (законченная фича/правка), не на каждый файл.
  Сообщение — по делу («добавил форму записи с валидацией»), НЕ пересказ реплики пользователя.
- **ОБЯЗАТЕЛЬНО сохрани (commit+push) ПЕРЕД тем как сказать «готово».** Пока не запушил —
  работа не сохранена, при перезапуске потеряется, и человек не увидит её в превью
  (превью показывает последнюю СОХРАНЁННУЮ версию, см. skill `preview`).
- **Перед push** — убедись, что собирается (`make check` / `go build ./...`). Сломанный код
  в сохранённой версии = превью застынет на прошлой рабочей.
- **Откат** («верни как было»): `git log --oneline` → `git revert <commit>` (одну правку) или
  `git reset --hard <commit>` (к версии) → `git push` (после reset — `git push --force`).
- **Пользователю про git/коммиты/push НЕ рассказывай** (это техническая кухня, см. «Общение»):
  «сохранил», «вернул как было», «откатил последнее».

## Не делать

- Не вводить ORM, фреймворки, Node/npm/JS-сборку или client-side SPA-фреймворки
  (React/Vue и т.п.) — стэк намеренно минимален. SPA-ОЩУЩЕНИЕ (без перезагрузки) даём
  через HTMX, а НЕ через JS-фреймворк со сборкой.
- Не писать в БД мимо репозиториев (SQL живёт только в repository/pgrepo).
- Не описывать схему через inline `CREATE TABLE` в коде — только файлы миграций
  (см. раздел «Миграции»).
- Не возвращать полную страницу из мутаций (ломает HTMX-паттерн).
- Не делать действий с полной перезагрузкой страницы (классический form-submit +
  редирект) — нарушает железное правило SPA-без-перезагрузки. Всё через hx-атрибуты.
- Не протаскивать SQL/HTTP в domain/usecase.
- Не переписывать аутентификацию/настройки с нуля — они уже в шаблоне.
